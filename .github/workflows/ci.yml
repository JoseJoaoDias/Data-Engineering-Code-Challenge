name: CI Pipeline

on:
  push:
    branches:
      - main
      - 'challenge_dev/*'
  
  pull_requets:
      branches:
      - main
      - 'challenge_dev/*'
  

  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.x'  # Specify the Python version you need

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt  # Assuming you have a requirements.txt file

      - name: Run tests
        run: |
          pytest tests/  # Adjust if using a different testing framework

      - name: Linting
        run: |
          flake8 .  # Assuming you're using flake8 for linting

      - name: Build and package
        run: |
          python setup.py sdist bdist_wheel  # For packaging your project
        
      - name: Upload build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: build-artifacts
          path: dist/

          
on:
  push:
    branches:
      - main
      - 'feature/*'

  pull_request:
    branches:
      - main
      - 'feature/*'
  
  workflow_dispatch:

jobs:
  setup:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Set up Python
      - name: Set up Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Step 4: Run tests
      - name: Run tests
        run: |
          python -m pytest tests.py

  main_process:
    runs-on: ubuntu-latest
    needs: setup

    steps:
      # Step 1: Checkout the code
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Set up Python
      - name: Set up Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Step 4: Run the main process
      - name: Run the main process
        run: |
          python main.py  # Run your main script

      # Step 5: Upload output files as artifacts
      - name: Upload output files
        uses: actions/upload-artifact@v3
        with:
          name: output-files
          path: ./data/output/*


          name: PySpark CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set Up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install PySpark
      run: pip install pyspark

    - name: Install Chispa
      run: pip install chispa

    - name: Install Other Dependencies
      run: pip install -r requirements.txt

    - name: Run Tests
      run: pytest --maxfail=1 --disable-warnings -q

    - name: Run Linter (flake8)
      run: |
        pip install flake8
        flake8 src/ tests/ --max-line-length=120

    - name: Run Code Formatter (black)
      run: |
        pip install black
        black --check src/ tests/